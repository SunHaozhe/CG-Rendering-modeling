#include "AxisAlignedBoundingBox.h"
/*
Vec3f AxisAlignedBoundingBox::tri_centroid(Triangle three_indices, const Mesh & mesh){
  Vec3f p1 = mesh.positions()[ three_indices[0] ];
  Vec3f p2 = mesh.positions()[ three_indices[1] ];
  Vec3f p3 = mesh.positions()[ three_indices[2] ];
  return (p1 + p2 + p3) / 3.0f;
}
*/
